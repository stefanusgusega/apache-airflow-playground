[project]
name = "apache-airflow-playground"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "apache-airflow==3.0.2",
]

[dependency-groups]
dev = [
    "black>=25.1.0",
    "icecream>=2.1.4",
]
